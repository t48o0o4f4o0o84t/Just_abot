name: Run Bot
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install libraries
        run: pip install pyTelegramBotAPI fuzzywuzzy python-Levenshtein requests
      - name: Run Bot
        run: python just_abot.py
        env:
          TOKEN: ${{ secrets.TOKEN }}
          
